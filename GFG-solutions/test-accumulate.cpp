#include<bits/stdc++.h>
// #include<iostream>
// #include<numeric>
// #include<stack>
// #include<vector>
// #include<algorithm>
using namespace std;

int main()
{
    // int n; cin >> n;
    // int a[n];
    // for(int i = 0; i < n; i ++)cin >> a[i];
    // int s = 0;
    // s = accumulate(a+1, a+3 , s);
    // cout << s;
    // stack<int> s;
    // cout << s.top();

    // int v[1000]; 
    // memset(v, -1, sizeof(v));
    // for(int i=0; i<7; i++)cout << v[i] << " "; // op - -1 -1 -1 -1 -1- 1 -1 -1 

    // string x, s = "123"; x = "";
    // x += s[0]; x += s[1];
    // if(s[0] + 1 != s[0+1])cout << "y";
    // else if(x == "12")cout << "n";

    //Bit testing 
    // int n=6, x=1;
    // n = n &1;
    // cout << n;
    
    string x = "abcs";
    cout << x.substr(0,1);
    return 0;


}