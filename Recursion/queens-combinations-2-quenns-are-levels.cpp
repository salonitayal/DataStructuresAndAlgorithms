#include<bits/stdc++.h>
using namespace std;
int cnt=0;
void queenCombinations(int qsf, int tb, int r, int c, string asf, int ssf, int ts, vector<int> queens)
{
    for(int j=nc; j<n; j++)
    {
        if()
    }
}
int main()
{
    int n, itms; cin >> n >> itms;
    vector<int> queens(itms);
    queenPermutations(0, n, 0, 0, "", 0, itms, queens);
    
    return 0;
}

